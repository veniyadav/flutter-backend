import { AxiosInstance } from 'axios';
export declare abstract class OneSignalBaseClient {
    protected BaseUrl: string;
    private _httpClient;
    get httpClient(): AxiosInstance;
    protected abstract createHttpClient(): any;
    private configureHttpClient;
    private createHttpClientInternal;
}
